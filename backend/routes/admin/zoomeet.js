const express = require("express");
const router = express.Router();
const zoomController = require("../../controllers/admin/zoomController");
const upload = require("../../middlewares/uploadMiddleware");

module.exports = router;
